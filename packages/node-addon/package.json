{
  "main": "index.js",
  "dependencies": {
    "node-addon-api": "^8.5.0",
    "node-gyp": "^12.2.0"
  },
  "scripts": {
    "install": "node-gyp rebuild && node-gyp configure -- -f gyp.generator.compile_commands_json.py && cp ./build/Release/compile_commands.json ./build"
  }
}
